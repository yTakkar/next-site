import Layout from '~/components/Layout'

export const meta = {
  title: 'Navigate Between Pages',
  courseId: 'basics',
  lessonId: 'navigate-between-pages',
  stepId: 'simple-but-powerful'
}

## Link is Simple, but Powerful

Here we've only looked at the very basic usage of `next/link`. There are some interesting ways to use it, and we'll learn about them in upcoming lessons.

In the meantime, have a look at the [Next.js Routing documentation](https://nextjs.org/docs/#routing). You'll find it useful.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
