import Layout from '~/components/Layout'

export const meta = {
  title: 'Lazy Loading Components',
  courseId: 'excel',
  lessonId: 'lazy-loading-components',
  stepId: 'analyze-result'
}

## Analyze Result

As you have seen, it's included in all `/pages/p/[page-id].js` bundles. That's because it's used only in individual pages.

![](/learn/static/images/llc-analyzer.png)

In this result page, we can't even see the `react-highlight` module. That's because it's pretty small. The module `highlight.js`, which is used inside it, is the heavy module.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
