import Layout from '~/components/Layout'
import AnswerBox from '~/components/AnswerBox'

export const meta = {
  title: 'Lazy Loading Components',
  courseId: 'excel',
  lessonId: 'lazy-loading-components',
  stepId: 'lets-analyze',
  question: {
    answers: [
      'inside static/runtime/main[a-random-string].js bundle',
      'inside static/chunks/commons[a-random-string].js bundle',
      'inside static/chunks/app[a-random-string].js bundle',
      'in all /pages/p/[page-id].js bundles'
    ],
    correctAnswer: 'in all /pages/p/[page-id].js bundles'
  }
}

## Let's Analyze

Before we do anything else, let's analyze our app's JavaScript bundles.
For that, simply run the following command:

```sh
npm run analyze:browser
```

After the command completes, you can see the results in the web browser.
Where is the `highlight.js` module included?

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
