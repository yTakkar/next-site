import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'Deploying a Next.js App',
  courseId: 'basics',
  lessonId: 'deploying-a-nextjs-app'
};

[▲ZEIT Now](https://zeit.co/now) is the most simple and scalable way to deploy your application to production.

However, you can deploy a Next.js app in any environment you can run Node.js &mdash; and it's relatively simple.

This means there's no vendor lock-in!

Let's learn about deploying Next.js apps.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
