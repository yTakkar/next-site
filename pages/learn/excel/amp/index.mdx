import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'Create AMP Pages',
  courseId: 'excel',
  lessonId: 'amp'
};

Next.js supports hybrid and [AMP](https://amp.dev) only pages, this tutorial shows you how to use both.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
