import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'Create AMP Pages',
  courseId: 'excel',
  lessonId: 'amp'
};

Next.js supports hybrid and AMP only pages, this tutorial shows you how to use both.

Before getting started, make sure you are using Next.js 8.1 or newer.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
