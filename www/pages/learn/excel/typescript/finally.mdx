import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'TypeScript',
  courseId: 'excel',
  lessonId: 'typescript',
  stepId: 'finally'
};

## Finally

Now with Next.js you can leverage the power of TypeScript without any issues, every module and lib imported from Next.js will also be 100% typed.

If you want to know more about TypeScript + Next.js here are some good examples:

- [with-typescript](https://github.com/zeit/next.js/tree/canary/examples/with-typescript)
- [with-jest-typescript](https://github.com/zeit/next.js/tree/canary/examples/with-jest-typescript)

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
